version: '2'

services:
  nginx:
    build:
      context: .
      dockerfile: conf/Dockerfile.production.nginx
    links:
      - application
    ports:
      - "80:8080"

  application:
    build:
      context: .
      dockerfile: conf/Dockerfile.production.app
